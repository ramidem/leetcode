/**
 * Give the array `nums` consisting of `2n` elements in the form:
 * [x1, x2, ..., xn, y1, y2, ..., xn, yn]
 *
 * Return the array in the form:
 * [x1, y1, x2, y2, ..., xn, yn]
 *
 * @param {number[]} num
 * @param {number} n
 * @return {number[]}
 */

let shuffle = function (nums, n) {
  return nums, n;
};

console.log(shuffle([1, 2, 3, 4, 5, 6, 7], 2));
